
<h1><%= @card.name %></h1>
<div class="card"><%= image_tag @card.img_url %></div>
<%= form_for @deck_card do |f| %>
<%= f.collection_select :deck_id, @decks, :id, :name %>
<%= f.hidden_field :card_id, :value => @card.id %>
<%= f.submit "Add to Deck" %>
<% end %>

